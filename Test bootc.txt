Dans un terminal de la VM, colle tout ça :

sudo bootc status
systemctl --failed
systemctl is-active scx_loader ananicy-cpp firewalld chronyd
echo $SHELL
ls /usr/bin/setup-second-disk.sh
journalctl -b -1 -p err 2>/dev/null | tail -15
Et crée la config snapper test :

sudo snapper -c root create-config /
sudo snapper ls
